source "https://rubygems.org"

# GitHub Pages build environment. This gem pins Jekyll, the Minima theme,
# and the plugins to the exact versions GitHub Pages runs.
gem "github-pages", group: :jekyll_plugins
